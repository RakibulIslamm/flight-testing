import React from 'react';

const Loading = () => {
    return (
        <div className="w-full min-h-screen flex justify-center items-center -mt-16">
            <h2 className="text-4xl font-semibold text-gray-300">Loading...</h2>
        </div>
    );
};

export default Loading;